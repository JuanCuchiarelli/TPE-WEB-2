<h1>Bandas</h1>

<ul>
    <?php foreach($bandas as $banda){ ?>
        <li>
            <a href="<?= BASE_URL ?>conciertos-banda/<?= $banda->id_banda ?>">
                <?= $banda->nombre ?>
            </a>
        </li>
    <?php } ?>
</ul>